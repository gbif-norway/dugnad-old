title: Vascular plants from Telemark - transcription
help:
  introduction: Transkriberingen skal i størst mulig grad gjøres verbatimt. Det betyr at dere følger slavisk innholdet på etiketten, men de må likevel plasseres i de aktuelle feltene. Vi har likevel av historiske og datatekniske årsaker noen tilpasninger som blir beskrevet nedenfor.
source:
  database: projects.db
  table: telemark
key: "occurrenceID"
annotate:
  order: [ event, taxonomy, geography, comment ]
filter: filter
forms:
  event:
    - { name: 'occurrenceID', type: hidden }
    - { name: 'recordedBy', type: text, help: "help-recordedBy" }
    - { name: "eventDate", type: date, help: "help-eventDate" }
  geography:
    - { name: "country", type: text }
    - { name: "stateProvince", type: text }
    - { name: "county", type: text, help: "help-county" }
    - { name: "locality", type: text, help: "help-locality" }
    - { name: "verbatimCoordinates", type: text, help: "help-coordinates" }
  taxonomy:
    - { name: "scientificName", type: text, help: 'help-scientificName' }
  comment:
    - { name: "multipleIdentifications", type: checkbox, help: "help-identifications" }
    - { name: "annotationRemarks", type: textfield }
  filter:
    - { name: 'genus', type: select, sql: 'SELECT DISTINCT "genus" from telemark ORDER BY "genus"', default: [ '', '~ Genus ~' ] }
    - { name: '_labelType', type: select, sql: 'SELECT DISTINCT "_labelType" from telemark ORDER BY "_labelType"', default: [ '', '~ Label Type ~' ] }
    - { name: 'recordedBy', type: select, sql: 'SELECT DISTINCT "recordedBy" from telemark ORDER BY "recordedBy"', default: [ '', '~ Collector ~' ] }
submit: transcribe
